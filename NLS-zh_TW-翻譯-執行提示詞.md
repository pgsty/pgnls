# PostgreSQL NLS zh_TW 翻譯執行提示詞 —— PG19 首輪

你是 zh_TW 消息翻譯的執行 agent。本輪目標:**PG19(`zh_TW/master`,28 個組件,12,638 條)達到可交付狀態**——全部譯出、零 fuzzy、機器閘門全綠、與官方既有譯文對齊。流程是兩階段:先用審查過的轉換管線打底,再對特定問題逐項解決。不得跳過階段二直接交付機器草稿。

## 1. 現狀與輸入

`zh_TW/master` 現有三種條目形態(來自 babel 對齊快照,官方 zh_TW 按 main 的 msgid 集合併):

| 形態 | 數量 | 處置 |
|:---|---:|:---|
| 已譯(msgid 與官方一致,官方譯文) | 6,855 | **不動**。R0.5 對齊義務:差異只記錄回饋,不私改 |
| fuzzy(上游措辭漂移,帶舊官方譯文) | 3,771 | 對照新 msgid 逐條修訂,完成後摘 fuzzy 旗標 |
| 未譯 | 2,012 | 階段一由管線起草,階段二審校 |

**必讀輸入**(按序):

1. `ref/style-guide-tw.md` —— 先通讀 **R0**(校準層:用字、用語對照、行/列/筆、R0.4 管線與禁字表、R0.5 官方對齊義務),再按需查 R1–R14。
2. `ref/glossary-tw.tsv` —— 637 條術語,`zh_TW` 欄是目標詞形;`依據` 欄標 官方/官方推定/MS/裁定/字符。
3. `ref/phrasebook-tw.tsv` —— 95 條固定句式;`zh_forbidden` 欄同時是機器轉換偽影禁字表。
4. `zh_CN/master/*.po` —— 同 msgid 的簡體譯文,是語義依據(術語按 TW 表轉,語義忠實度以它與英文為準)。

工具:`bin/s2tw.py`(轉換管線;規則表與 `tmp/build_tw_glossary.py` 同源,2026-09-18 對抗性審查後版本)。

## 2. 階段一:OpenCC 打底

```bash
python3 bin/s2tw.py --branch master --verify
```

- 只填**未譯**條目:取 zh_CN 同 msgid 譯文 → 術語規則(單遍、最長優先、區段保護)→ OpenCC `s2twp` 殘差 → 偽影後修。填充條目一律標 `#, fuzzy`(機器草稿旗標)。
- 工具保證:不碰 header、不碰已譯、不碰舊 fuzzy、不碰 `#~` 廢棄條目;`--verify` 會以去 fuzzy 夾具跑 `msgfmt --check --check-format`,失敗即中止。
- 預期:28 檔填充約 2,080 條。跑完記錄每組件填充數。

**此階段產物一律是草稿。** 禁止在此階段摘任何 fuzzy 旗標。

## 3. 階段二:特定問題特定解決

逐項執行,每項獨立可驗證;修復按 R13 紀律(最小改動、逐條閱讀,不用正則批量替換代替閱讀)。

**(a) 偽影禁字表掃描(L10-TW)** —— 全量掃 msgstr,出現即錯(R0.4 表):
`運算符、排序規則、歸檔、死鎖、遊標、保存點、所有者、主頁、集群、集簇、外部表、使用者對應、使用者對映、許可權、引數、後臺、高速緩存、連線字串、插槽、運運算元、演演算法、資料分割、退出`。
處置:按 `ref/glossary-tw.tsv` 的正確詞形改;`資料分割→分割`。

**(b) 簡體殘字掃描(L17)** —— 對每個 msgstr 中的漢字 c:若 `s2t.convert(c) != c` 且 `t2s.convert(c) == c` 則為殘字,出現即錯(白名單除外:`游群后里面台只系制表发复干冲划尽历志余准征致云板采松周累脏卷汇斗`)。官方已譯條目裡發現的殘字也要修(如官方偶用簡體字),逐條記錄。

**(c) 行/列/筆 語境(L18)** —— 逐條復核含 `行`/`列` 的譯文:row 指稱 → `資料列`;row 計數(`%d 行`、`(1 row)`)→ `筆`(官方:`(%lu 筆資料)`,半形括號);column → `欄/欄位`;line(行號)→ `行`。禁 MS 式 `資料行`。

**(d) 信息 二分** —— message 語境 → `訊息`;information 語境 → `資訊`(`版本資訊`、`詳細資訊`、`統計資訊`)。機器草稿與舊官方條目都要過這一項。

**(e) 高頻句式對照(R3.2)** —— 抽查核對:`could not/cannot/unable to → 無法`、`must not → 不得`、`unexpected → 非預期`(注意:與簡體版裁定「意外」相反)、`exit → 結束`、`shut down → 停止`、`timeout → 逾時`、`permission denied → 權限不足`、`expected X, got Y → 預期為 X,實際為 Y`。全表見 `ref/phrasebook-tw.tsv`。

**(f) 舊官方 fuzzy 條目修訂(3,771 條,主工作量)** —— 每條:讀新 msgid 的變化點(通常是參數、措辭微調),在舊官方譯文骨架上最小修訂;官方術語用語保留(行程、待命伺服器、熱備援、備份、封存、分割、表空間…)。修訂後按閘門校驗再摘 fuzzy。無法確定的按 R2.5 給保守譯文並記疑問。

**(g) 官方對齊異議清單** —— 已譯條目不動,但已知差異要記錄備回馈:官方 `詳細內容:` vs 我方指南 `詳細資訊:`(R9.2);官方 pg_dump `外鍵檢查規則`(constraint 誤譯痕跡);官方 pg_dump 內 `分區` vs psql `分割`;官方 `訊息/資訊` 混用處。匯總成一份 pgsql-translators 回饋草稿,放在報告裡。

**(h) 標點、空格、對齊(R4–R6)** —— 中英之間半形空格;全形標點兩側無空格;引號一律 ASCII `"`;佔位符與量詞之間留空格(`%d 位元組`)。選項說明行按顯示寬度對齊(漢字寬 2):`bin/check-align.py` 目前只查 zh_CN,zh_TW 需以相同規則複核涉及對齊的組件(pg_controldata、pg_resetwal、initdb 摘要、--help 選項行)。

**(i) 術語全量對照** —— 以 `ref/glossary-tw.tsv` 逐組件過一遍高頻名詞(資料表、交易、行程、函式、型別、欄位、定序、區域、擴充模組、外部資料表、快取、記憶體、伺服器、使用者、權限、檔案、目錄、參數、變數、物件、檢視、聯結、佇列、死結、儲存點、觸發器、檢查點、日誌、備份、還原、復原、封存、串流、複寫、訂閱、發布、叢集、表空間、schema/socket 原文)。

## 4. 閘門(每個組件完成時必須全綠)

```bash
# 1. 語法與格式(以去 fuzzy 夾具覆蓋全部條目)
msgfmt --check --check-format -o /dev/null zh_TW/master/<組件>.po

# 2. msgid 集與 zh_CN 完全一致(不丟不增不錯位)
msgcmp zh_TW/master/<組件>.po zh_CN/master/<組件>.po

# 3. fuzzy 歸零、無未譯
msgfmt --statistics zh_TW/master/<組件>.po -o /dev/null   # 應 100% translated, 0 fuzzy

# 4. 禁字表掃描 0 命中;簡體殘字掃描 0 命中(白名單除外)
```

全 28 組件完成後:`python3 bin/check.py`(zh_CN 側不回歸)+ 匯總報告。

## 5. 紀律與產出

- **R13 最小改動**:每處修改對應一個明確問題;分類計數 `term / punct / space / align / semantic / new`。
- 官方已譯 6,855 條的任何異議 → 進回饋清單,不入庫。
- 報告寫到 `reports/`(本地工作區,不提交):三態起點統計、階段一填充數、階段二各項修訂數、異議清單、遺留問題(逐條附 msgid 與理由)。
- 術語表/風格指南如發現錯誤:先核英文原意,修 `ref/` 並在報告記錄,不在譯文裡私自繞開。

## 6. 禁止事項

1. 禁止把 `ref/glossary-tw.tsv` 的 zh_CN→zh_TW 兩列當 sed 鏈順序替換,或用單字行(表/行/列/位/块/堆/頁/鎖)做字符串替換 —— 只允許 `bin/s2tw.py` 的單遍引擎(R0.4,2026-09-18 審查結論)。
2. 禁止改動 msgid、header、`#:` 參考行、`#~` 廢棄條目。
3. 禁止摘 fuzzy 旗標前跳過閘門;禁止批量正則替代逐條閱讀(R13.7)。
4. 禁止在未記錄的情況下改動官方已譯條目。
5. 本輪只做 `zh_TW/master`(PG19);其他五個版本目錄不動,留待後續輪次按同一提示詞重跑。
